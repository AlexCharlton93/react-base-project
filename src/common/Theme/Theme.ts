import { createMuiTheme } from "@material-ui/core";

const Theme = createMuiTheme({
  spacing: 8,
  palette: {
    primary: {
      main: "#007bff",
    },
    secondary: {
      main: "#007bff",
    },
  }
});

export default Theme;
