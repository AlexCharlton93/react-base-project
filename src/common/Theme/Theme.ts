import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
  spacing: 8,
  palette: {
    primary: {
      main: "#007bff",
    },
    secondary: {
      main: "#007bff",
    },
  }
});

export default Theme;
